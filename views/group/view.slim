= content_for :head
  title =group.name
  ==stylesheet_link_tag :base

= content_for :header
  h1 =group.name

ul
  - group.members.each do |member|
    li =member.email

a href="/poll/create?group_id=#{group.id}" Create Poll
